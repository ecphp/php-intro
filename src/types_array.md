## Array

> An array in PHP is actually an ordered map.<br />
A map is a type that associates values to keys.

-- *php.net*

```
<?php
$a = array(
  "foo" => "bar",
  "bar" => "baz",
);

$b = [
  "foo" => "bar",
  "bar" => "baz",
];
?>
```
